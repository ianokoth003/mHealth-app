# HealthEase Backend (Flask + MongoDB)

## Prerequisites
- Python 3.9+
- MongoDB running locally (default URI: mongodb://localhost:27017)
- Git (optional)

## Quick start (Windows PowerShell)
1. Open PowerShell
2. cd to backend folder:
